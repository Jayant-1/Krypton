# ── Production Frontend Configuration ─────────────────────────────────────────
# Must match the backend ALLOWED_ORIGINS for CORS to work

# 🔒 CRITICAL: Update to your actual production backend URL
# This is what the browser will use to fetch data
VITE_API_BASE_URL=https://krypton-ryngvlpb.b4a.run
